@model List<CompanyManagement.Models.Transaction>
@{
    ViewBag.Title = "كارت صنف - " + Model[0].Product.name;
}

<h2>كارت صنف: @Model[0].Product.name</h2>
<hr />
<table class="table table-bordered">
    <thead class="thead-light">
        <tr>
            <th>رصيد</th>
            <th>وارد</th>
            <th>منصرف</th>
            <th>سعر التكلفة</th>
            <th>التاريخ</th>
            <th>البيان</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var tran in Model)
        {
            <tr>
                <td>@tran.balance</td>
                <td>@tran.income</td>
                <td>@tran.outcome</td>
                <td>@tran.unit_cost</td>
                <td>@tran.move_date</td>
                <td>
                    <a href="/Orders/Details/@tran.Order.custom_order_id">
                        @tran.Order.custom_order_id
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

